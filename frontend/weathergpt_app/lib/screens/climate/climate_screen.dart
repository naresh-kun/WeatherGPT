/// WeatherGPT — Climate Screen
/// Historical climate trend charts. Not yet implemented.
library;

import 'package:flutter/material.dart';

class ClimateScreen extends StatelessWidget {
  const ClimateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('WeatherGPT — Climate Trends')),
      body: const Center(child: Text('Climate Screen — not yet implemented')),
    );
  }
}
