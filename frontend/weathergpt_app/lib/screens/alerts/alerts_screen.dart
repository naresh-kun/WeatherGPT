/// WeatherGPT — Alerts Screen
/// Active weather alerts display. Not yet implemented.
library;

import 'package:flutter/material.dart';

class AlertsScreen extends StatelessWidget {
  const AlertsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('WeatherGPT — Alerts')),
      body: const Center(child: Text('Alerts Screen — not yet implemented')),
    );
  }
}
