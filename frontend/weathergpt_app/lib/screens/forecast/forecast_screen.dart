/// WeatherGPT — Forecast Screen
/// Multi-day weather forecast display. Not yet implemented.
library;

import 'package:flutter/material.dart';

class ForecastScreen extends StatelessWidget {
  const ForecastScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('WeatherGPT — Forecast')),
      body: const Center(child: Text('Forecast Screen — not yet implemented')),
    );
  }
}
