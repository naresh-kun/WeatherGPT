/// WeatherGPT — Chat Screen
/// Conversational weather query interface. Not yet implemented.
library;

import 'package:flutter/material.dart';

class ChatScreen extends StatelessWidget {
  const ChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('WeatherGPT — Chat')),
      body: const Center(child: Text('Chat Screen — not yet implemented')),
    );
  }
}
