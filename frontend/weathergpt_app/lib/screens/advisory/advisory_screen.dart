/// WeatherGPT — Advisory Screen
/// Weather-based advisories display. Not yet implemented.
library;

import 'package:flutter/material.dart';

class AdvisoryScreen extends StatelessWidget {
  const AdvisoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('WeatherGPT — Advisory')),
      body: const Center(child: Text('Advisory Screen — not yet implemented')),
    );
  }
}
